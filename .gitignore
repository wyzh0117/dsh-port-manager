# ── 依赖与包管理器 ────────────────────────────────────────────────────────────
# 本包宿主半零 import、浏览器半只 require("react")，正常开发不需要 node_modules；
# 只有跑 lint / 本地渲染断言时才会生成，一律不入库。
node_modules/
.pnpm-store/
.npm-cache/
.yarn/
.pnp.*
*.tgz

# ── 构建 / 打包产物 ──────────────────────────────────────────────────────────
# lib/ 是手写源码（不是构建输出），因此这里只挡真正的产物目录。
dist/
build/
out/
coverage/
*.tsbuildinfo

# ── 测试产物 ─────────────────────────────────────────────────────────────────
.nyc_output/
junit.xml
test-results/

# ── 编辑器 / IDE ─────────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~

# ── 操作系统 ─────────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini

# ── 日志与本地临时文件 ───────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
tmp/
temp/
*.tmp

# ── 本地环境 / 密钥 ──────────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key

# ── 开发过程文件 ─────────────────────────────────────────────────────────────
# 调研报告、审查记录、AI 助手工作目录等：属于开发过程产物，不属于产品。
.briefs/
.recon/
.dsh-upgrade-backup/
.graft/
*.local.md

# ── 补丁残留 ─────────────────────────────────────────────────────────────────
*.orig
*.rej
